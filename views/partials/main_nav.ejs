  <nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-secondary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/home"><img class="logo-img" src="/Assets/images/logo.png"></i></i> Home</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto"> 
          <li>
            <% if (title == "About me") { %>
            <a class="nav-link active" aria-current="page" href="/about">
            <% } else { %>
            <a class="nav-link" aria-current="page" href="/about">
            <% } %>
            <i class="fas fa-portrait"></i></i> Abut me</a>
          </li>
          <li class="nav-item">
            <% if (title == "Projects") { %>
            <a class="nav-link active" href="/projects">
            <% } else { %>
            <a class="nav-link" href="/projects">
            <% } %>          
            <i class="fas fa-align-justify"></i> Projects</a>
          </li>
          <li class="nav-item">
            <% if (title == "Services") { %>
            <a class="nav-link active" href="/services">
            <% } else { %>
            <a class="nav-link" href="/services">
            <% } %>          
            <i class="fas fa-cogs"></i> Services</a>
          </li>
          <li class="nav-item">
            <% if (title == "Contact") { %>
            <a class="nav-link active" href="/contactme">
            <% } else { %>
            <a class="nav-link" href="/contactme">
            <% } %>
            <i class="fas fa-comments-dollar"></i> Contact Me</a>
          </li>
          <li class="nav-item">
            <% if (title == "Login") { %>
            <a class="nav-link active" href="/login">
            <% } else { %>
            <a class="nav-link" href="/login">
            <% } %>
            <i class="fas fa-sign-in-alt"></i>Login</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>